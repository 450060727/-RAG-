<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoma.server.mapper.admin.SysMenuMapper">

    <resultMap id="BaseResultMap" type="com.xiaoma.server.entity.admin.SysMenu">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="permission" property="permission"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        m.id, m.parent_id, m.name, m.type, m.permission, m.path, m.component, m.icon, m.sort, m.status, m.created_at, m.updated_at
    </sql>

    <select id="selectByRoleIds" resultMap="BaseResultMap">
        SELECT DISTINCT <include refid="Base_Column_List"/>
        FROM `sys_menu` m
        INNER JOIN `sys_role_menu` rm ON m.id = rm.menu_id
        WHERE rm.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        AND m.status = 0
        ORDER BY m.sort ASC, m.id ASC
    </select>

    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        SELECT menu_id
        FROM `sys_role_menu`
        WHERE role_id = #{roleId}
    </select>

</mapper>
