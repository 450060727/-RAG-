<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoma.server.mapper.kb.KbSegmentMapper">

    <insert id="insertBatch">
        INSERT INTO kb_segment
        (doc_id, category_id, content, vector_id, sort_order, status, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.docId}, #{item.categoryId}, #{item.content}, #{item.vectorId},
             #{item.sortOrder}, #{item.status}, #{item.createdAt})
        </foreach>
    </insert>

</mapper>
