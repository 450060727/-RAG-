<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoma.server.mapper.admin.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.xiaoma.server.entity.admin.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="dept_id" property="deptId"/>
        <result column="status" property="status"/>
        <result column="super_admin" property="superAdmin"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, real_name, phone, email, avatar, dept_id, status, super_admin, last_login_time, created_at, updated_at
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `sys_user`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM `sys_user`
        WHERE username = #{username}
    </select>

</mapper>
